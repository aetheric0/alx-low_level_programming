#include <stdio.h>

/**
 * main - Program Starts here
 *
 * Return: return value is 0
 */

int main(void)
{
	int c, d;

	for (c = 48; c <= 57; c++)
		putchar(c);
	for (d = 97; d <= 102; d++)
		putchar(d);
	putchar('\n');

	return (0);
}
