#include "main.h"

void print_alphabet(void);

int main(void)
{
	print_alphabet();
	return (0);
}

void print_alphabet(void)
{
	int alpha_lower;

	for (alpha_lower = 48; alpha_lower <= 57; alpha_lower++)
		_putchar(alpha_lower);
}
